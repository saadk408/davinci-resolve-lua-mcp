<!--
The title and this body become the squash commit on main. Write the body as the commit message:
what changed and why. Delete the checklist before merging, or leave only what belongs in history.
-->

## Why



## What changed



## Checks

- [ ] `make test` and `make lint-lua` pass locally (the Lua checks need a DaVinci Resolve install)
- [ ] `make bundle` reports the eight shipped files
- [ ] A change to the bridge or `claude_diag.lua` ran through `make smoke` on a scratch project
- [ ] Docs follow the behaviour: README for users, CLAUDE.md for the design record
